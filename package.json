{
  "name": "face-rec-app",
  "version": "1.0.0",
  "description": "Api for an application that detects faces in the images you upload to it.",
  "scripts": {
    "frontend": "cd frontend && npm run dev",
    "server": "nodemon server.js",
    "build": "npm install --only=prod && cd frontend && npm install --only=prod && npm run build",
    "dev": "concurrently --kill-others-on-fail \"npm run server\" \"npm run firebase-dev\" \"npm run dev-local\"",
    "start": "node server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "compression": "^1.7.5",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "express-sslify": "^1.2.0",
    "morgan": "^1.10.0"
  }
}
